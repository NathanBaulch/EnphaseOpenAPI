# do not import all endpoints into this module because that uses a lot of memory and stack frames
# if you need the ability to import all endpoints from this module, import them with
# from commissioning.paths.activations_activation_id_meters_serial_number_meter_control import Api

from commissioning.paths import PathValues

path = PathValues.ACTIVATIONS_ACTIVATION_ID_METERS_SERIAL_NUMBER_METER_CONTROL