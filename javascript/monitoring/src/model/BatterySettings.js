/**
 * The Enphase Monitoring API
 * Monitoring API can be used to fetch system details on a site, system-level production and consumption data, and device-level production data.
 *
 * The version of the OpenAPI document: 4.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The BatterySettings model module.
 * @module model/BatterySettings
 * @version 4.0
 */
class BatterySettings {
    /**
     * Constructs a new <code>BatterySettings</code>.
     * @alias module:model/BatterySettings
     */
    constructor() { 
        
        BatterySettings.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>BatterySettings</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BatterySettings} obj Optional instance to populate.
     * @return {module:model/BatterySettings} The populated <code>BatterySettings</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BatterySettings();

            if (data.hasOwnProperty('system_id')) {
                obj['system_id'] = ApiClient.convertToType(data['system_id'], 'Number');
            }
            if (data.hasOwnProperty('battery_mode')) {
                obj['battery_mode'] = ApiClient.convertToType(data['battery_mode'], 'String');
            }
            if (data.hasOwnProperty('reserve_soc')) {
                obj['reserve_soc'] = ApiClient.convertToType(data['reserve_soc'], 'Number');
            }
            if (data.hasOwnProperty('energy_independence')) {
                obj['energy_independence'] = ApiClient.convertToType(data['energy_independence'], 'String');
            }
            if (data.hasOwnProperty('charge_from_grid')) {
                obj['charge_from_grid'] = ApiClient.convertToType(data['charge_from_grid'], 'String');
            }
            if (data.hasOwnProperty('battery_shutdown_level')) {
                obj['battery_shutdown_level'] = ApiClient.convertToType(data['battery_shutdown_level'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BatterySettings</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BatterySettings</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['battery_mode'] && !(typeof data['battery_mode'] === 'string' || data['battery_mode'] instanceof String)) {
            throw new Error("Expected the field `battery_mode` to be a primitive type in the JSON string but got " + data['battery_mode']);
        }
        // ensure the json data is a string
        if (data['energy_independence'] && !(typeof data['energy_independence'] === 'string' || data['energy_independence'] instanceof String)) {
            throw new Error("Expected the field `energy_independence` to be a primitive type in the JSON string but got " + data['energy_independence']);
        }
        // ensure the json data is a string
        if (data['charge_from_grid'] && !(typeof data['charge_from_grid'] === 'string' || data['charge_from_grid'] instanceof String)) {
            throw new Error("Expected the field `charge_from_grid` to be a primitive type in the JSON string but got " + data['charge_from_grid']);
        }

        return true;
    }


}



/**
 * @member {Number} system_id
 */
BatterySettings.prototype['system_id'] = undefined;

/**
 * Current battery mode of the system. Available values are Savings Mode, Full Backup, and Self - Consumption.
 * @member {String} battery_mode
 */
BatterySettings.prototype['battery_mode'] = undefined;

/**
 * Reserve soc for the current mode.
 * @member {Number} reserve_soc
 */
BatterySettings.prototype['reserve_soc'] = undefined;

/**
 * Energy independence after peak hours enabled/disabled. Enabled: Battery will discharge to save money during peak hours when electricity rates are high. Once that is done, energy independence is prioritized. This will increase the energy self-sufficiency until the reserve soc is hit. Battery will charge from solar in the morning to get ready for the next period. Disabled: Battery will discharge only during peak hours when electricity rates are high. During other times, battery will be idle or will charge from solar in the morning to get ready for the next period.
 * @member {String} energy_independence
 */
BatterySettings.prototype['energy_independence'] = undefined;

/**
 * Charge battery from grid enabled/disabled. Enabled: Battery may also charge from the grid when in full backup mode or when battery charge is below the reserve capacity in savings or self-consumption mode.
 * @member {String} charge_from_grid
 */
BatterySettings.prototype['charge_from_grid'] = undefined;

/**
 * Level at which battery shutdowns to conserve energy. Battery restarts by using the conserved energy when the utility grid restores power or solar panels start producing energy.
 * @member {Number} battery_shutdown_level
 */
BatterySettings.prototype['battery_shutdown_level'] = undefined;






export default BatterySettings;

