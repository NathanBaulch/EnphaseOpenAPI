/**
 * The Enphase Monitoring API
 * Monitoring API can be used to fetch system details on a site, system-level production and consumption data, and device-level production data.
 *
 * The version of the OpenAPI document: 4.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Meta from './Meta';

/**
 * The GetSystemEnergyLifetimeResponse model module.
 * @module model/GetSystemEnergyLifetimeResponse
 * @version 4.0
 */
class GetSystemEnergyLifetimeResponse {
    /**
     * Constructs a new <code>GetSystemEnergyLifetimeResponse</code>.
     * @alias module:model/GetSystemEnergyLifetimeResponse
     */
    constructor() { 
        
        GetSystemEnergyLifetimeResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GetSystemEnergyLifetimeResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetSystemEnergyLifetimeResponse} obj Optional instance to populate.
     * @return {module:model/GetSystemEnergyLifetimeResponse} The populated <code>GetSystemEnergyLifetimeResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetSystemEnergyLifetimeResponse();

            if (data.hasOwnProperty('system_id')) {
                obj['system_id'] = ApiClient.convertToType(data['system_id'], 'Number');
            }
            if (data.hasOwnProperty('start_date')) {
                obj['start_date'] = ApiClient.convertToType(data['start_date'], 'Date');
            }
            if (data.hasOwnProperty('meter_start_date')) {
                obj['meter_start_date'] = ApiClient.convertToType(data['meter_start_date'], 'Date');
            }
            if (data.hasOwnProperty('production')) {
                obj['production'] = ApiClient.convertToType(data['production'], ['Number']);
            }
            if (data.hasOwnProperty('micro_production')) {
                obj['micro_production'] = ApiClient.convertToType(data['micro_production'], ['Number']);
            }
            if (data.hasOwnProperty('meter_production')) {
                obj['meter_production'] = ApiClient.convertToType(data['meter_production'], ['Number']);
            }
            if (data.hasOwnProperty('meta')) {
                obj['meta'] = Meta.constructFromObject(data['meta']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetSystemEnergyLifetimeResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetSystemEnergyLifetimeResponse</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['production'])) {
            throw new Error("Expected the field `production` to be an array in the JSON data but got " + data['production']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['micro_production'])) {
            throw new Error("Expected the field `micro_production` to be an array in the JSON data but got " + data['micro_production']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['meter_production'])) {
            throw new Error("Expected the field `meter_production` to be an array in the JSON data but got " + data['meter_production']);
        }
        // validate the optional field `meta`
        if (data['meta']) { // data not null
          Meta.validateJSON(data['meta']);
        }

        return true;
    }


}



/**
 * System ID.
 * @member {Number} system_id
 */
GetSystemEnergyLifetimeResponse.prototype['system_id'] = undefined;

/**
 * When no start_date parameter is specified on the request, this is the operational_date of the system. May be null if system has never produced. When a start_date parameter is included in the request, it is included here in the response.
 * @member {Date} start_date
 */
GetSystemEnergyLifetimeResponse.prototype['start_date'] = undefined;

/**
 * First day in the time series when measurements are taken from a meter instead of from microinverters. This field is not present unless the system has a meter.
 * @member {Date} meter_start_date
 */
GetSystemEnergyLifetimeResponse.prototype['meter_start_date'] = undefined;

/**
 * An array of production measurements, one for each day since the system started producing, or one for each day of the requested period. If the system has never produced energy, the array may be empty.
 * @member {Array.<Number>} production
 */
GetSystemEnergyLifetimeResponse.prototype['production'] = undefined;

/**
 * @member {Array.<Number>} micro_production
 */
GetSystemEnergyLifetimeResponse.prototype['micro_production'] = undefined;

/**
 * @member {Array.<Number>} meter_production
 */
GetSystemEnergyLifetimeResponse.prototype['meter_production'] = undefined;

/**
 * @member {module:model/Meta} meta
 */
GetSystemEnergyLifetimeResponse.prototype['meta'] = undefined;






export default GetSystemEnergyLifetimeResponse;

