/**
 * The Enphase Monitoring API
 * Monitoring API can be used to fetch system details on a site, system-level production and consumption data, and device-level production data.
 *
 * The version of the OpenAPI document: 4.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Meta from './Meta';

/**
 * The GetSystemConsumptionLifetimeResponse model module.
 * @module model/GetSystemConsumptionLifetimeResponse
 * @version 4.0
 */
class GetSystemConsumptionLifetimeResponse {
    /**
     * Constructs a new <code>GetSystemConsumptionLifetimeResponse</code>.
     * @alias module:model/GetSystemConsumptionLifetimeResponse
     */
    constructor() { 
        
        GetSystemConsumptionLifetimeResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GetSystemConsumptionLifetimeResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GetSystemConsumptionLifetimeResponse} obj Optional instance to populate.
     * @return {module:model/GetSystemConsumptionLifetimeResponse} The populated <code>GetSystemConsumptionLifetimeResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GetSystemConsumptionLifetimeResponse();

            if (data.hasOwnProperty('system_id')) {
                obj['system_id'] = ApiClient.convertToType(data['system_id'], 'Number');
            }
            if (data.hasOwnProperty('start_date')) {
                obj['start_date'] = ApiClient.convertToType(data['start_date'], 'Date');
            }
            if (data.hasOwnProperty('consumption')) {
                obj['consumption'] = ApiClient.convertToType(data['consumption'], ['Number']);
            }
            if (data.hasOwnProperty('meta')) {
                obj['meta'] = Meta.constructFromObject(data['meta']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GetSystemConsumptionLifetimeResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GetSystemConsumptionLifetimeResponse</code>.
     */
    static validateJSON(data) {
        // ensure the json data is an array
        if (!Array.isArray(data['consumption'])) {
            throw new Error("Expected the field `consumption` to be an array in the JSON data but got " + data['consumption']);
        }
        // validate the optional field `meta`
        if (data['meta']) { // data not null
          Meta.validateJSON(data['meta']);
        }

        return true;
    }


}



/**
 * Unique numeric ID of the system.
 * @member {Number} system_id
 */
GetSystemConsumptionLifetimeResponse.prototype['system_id'] = undefined;

/**
 * When no start_date parameter is specified on the request, defaults to the operational_date of the system. May be null if system has never produced. When a start_date parameter is included in the request, the same is included in the response.
 * @member {Date} start_date
 */
GetSystemConsumptionLifetimeResponse.prototype['start_date'] = undefined;

/**
 * An array of consumption measurements in Wh, one for each day since consumption metering began, or one for each day of the requested period.
 * @member {Array.<Number>} consumption
 */
GetSystemConsumptionLifetimeResponse.prototype['consumption'] = undefined;

/**
 * @member {module:model/Meta} meta
 */
GetSystemConsumptionLifetimeResponse.prototype['meta'] = undefined;






export default GetSystemConsumptionLifetimeResponse;

