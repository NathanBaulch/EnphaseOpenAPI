/**
 * The Enphase Commissioning API
 * Commissioning API can be used to create and update activations on a site, manage companies and users, update Tariff, etc.
 *
 * The version of the OpenAPI document: 4.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UpdateActivationEstimateRequest model module.
 * @module model/UpdateActivationEstimateRequest
 * @version 4.0
 */
class UpdateActivationEstimateRequest {
    /**
     * Constructs a new <code>UpdateActivationEstimateRequest</code>.
     * @alias module:model/UpdateActivationEstimateRequest
     * @param degradeFactor {String} Annual production degradation factor for the system, from year 0 of the system's life. Must be 0.0% or higher.
     * @param monthEstimates {Array.<String>} Estimated monthly production of the system, in kWh. A value for each month must be provided, and must be greater than 0. Send an empty array if you need to remove monthly estimates.
     */
    constructor(degradeFactor, monthEstimates) { 
        
        UpdateActivationEstimateRequest.initialize(this, degradeFactor, monthEstimates);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, degradeFactor, monthEstimates) { 
        obj['degrade_factor'] = degradeFactor;
        obj['month_estimates'] = monthEstimates;
    }

    /**
     * Constructs a <code>UpdateActivationEstimateRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UpdateActivationEstimateRequest} obj Optional instance to populate.
     * @return {module:model/UpdateActivationEstimateRequest} The populated <code>UpdateActivationEstimateRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UpdateActivationEstimateRequest();

            if (data.hasOwnProperty('degrade_factor')) {
                obj['degrade_factor'] = ApiClient.convertToType(data['degrade_factor'], 'String');
            }
            if (data.hasOwnProperty('month_estimates')) {
                obj['month_estimates'] = ApiClient.convertToType(data['month_estimates'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UpdateActivationEstimateRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UpdateActivationEstimateRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UpdateActivationEstimateRequest.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['degrade_factor'] && !(typeof data['degrade_factor'] === 'string' || data['degrade_factor'] instanceof String)) {
            throw new Error("Expected the field `degrade_factor` to be a primitive type in the JSON string but got " + data['degrade_factor']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['month_estimates'])) {
            throw new Error("Expected the field `month_estimates` to be an array in the JSON data but got " + data['month_estimates']);
        }

        return true;
    }


}

UpdateActivationEstimateRequest.RequiredProperties = ["degrade_factor", "month_estimates"];

/**
 * Annual production degradation factor for the system, from year 0 of the system's life. Must be 0.0% or higher.
 * @member {String} degrade_factor
 */
UpdateActivationEstimateRequest.prototype['degrade_factor'] = undefined;

/**
 * Estimated monthly production of the system, in kWh. A value for each month must be provided, and must be greater than 0. Send an empty array if you need to remove monthly estimates.
 * @member {Array.<String>} month_estimates
 */
UpdateActivationEstimateRequest.prototype['month_estimates'] = undefined;






export default UpdateActivationEstimateRequest;

